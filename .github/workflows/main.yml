
name: Deploy After Merge

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Prepare lab
        run: |
          echo "Setting up lab environment..."

      - name: Deploy
        run: |
          echo "Deploying..."
